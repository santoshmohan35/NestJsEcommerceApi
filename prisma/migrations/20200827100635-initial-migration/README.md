# Migration `20200827100635-initial-migration`

This migration has been generated by Santosh_Mohan at 8/27/2020, 3:36:35 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE `ecommerceapi`.`User` (
`id` int  NOT NULL  AUTO_INCREMENT,
`email` varchar(191)  NOT NULL ,
`password` varchar(191)  NOT NULL ,
`mobile` varchar(191)  NOT NULL ,
`firstname` varchar(191)  NOT NULL ,
`lastname` varchar(191)  NOT NULL ,
UNIQUE Index `User.email_unique`(`email`),
PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `ecommerceapi`.`Product` (
`id` int  NOT NULL  AUTO_INCREMENT,
`name` varchar(191)  NOT NULL ,
`description` varchar(191)  NOT NULL ,
`price` decimal(65,30)  NOT NULL ,
`imageUrl` varchar(191)  NOT NULL ,
`productBrandId` int  NOT NULL ,
`productTypeId` int  NOT NULL ,
PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `ecommerceapi`.`ProductBrand` (
`id` int  NOT NULL  AUTO_INCREMENT,
`name` varchar(191)  NOT NULL ,
PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `ecommerceapi`.`ProductType` (
`id` int  NOT NULL  AUTO_INCREMENT,
`name` varchar(191)  NOT NULL ,
PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

ALTER TABLE `ecommerceapi`.`Product` ADD FOREIGN KEY (`productBrandId`) REFERENCES `ecommerceapi`.`ProductBrand`(`id`) ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE `ecommerceapi`.`Product` ADD FOREIGN KEY (`productTypeId`) REFERENCES `ecommerceapi`.`ProductType`(`id`) ON DELETE CASCADE ON UPDATE CASCADE

DROP TABLE `ecommerceapi`.`_migration`

DROP TABLE `ecommerceapi`.`product`

DROP TABLE `ecommerceapi`.`productbrand`

DROP TABLE `ecommerceapi`.`producttype`

DROP TABLE `ecommerceapi`.`user`
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200827100635-initial-migration
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,44 @@
+// This is your Prisma schema file,
+// learn more about it in the docs: https://pris.ly/d/prisma-schema
+
+datasource db {
+  provider = "mysql"
+  url = "***"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model User {
+  id        Int     @default(autoincrement()) @id
+  email     String  @unique
+  password  String
+  mobile    String
+  firstname String
+  lastname  String
+}
+
+model Product {
+  id             Int          @default(autoincrement()) @id
+  name           String
+  description    String
+  price          Float
+  imageUrl       String
+  ProductBrand   ProductBrand @relation(fields: [productBrandId], references: [id])
+  productBrandId Int
+  productType    ProductType  @relation(fields: [productTypeId], references: [id])
+  productTypeId  Int
+}
+
+model ProductBrand {
+  id      Int       @default(autoincrement()) @id
+  name    String
+  Product Product[]
+}
+
+model ProductType {
+  id      Int       @default(autoincrement()) @id
+  name    String
+  Product Product[]
+}
```


